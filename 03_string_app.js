// String
/* const name = 'Alexey';
const age = 22;

const output = 'Привет меня зовут ' + name + ' и мой возрост ' + age + ' года';
const output2 = `Привет меня зовут ${name} и мой возрост ${age} года`;
console.log(output);
console.log(output2); */

/* function logPerson(s, name, age) {
  console.log(s, name, age);
  if (age < 0) {
     age = 'Ещё не родился';
  }
  return `${s[0]}${name}${s[1]}${age}`;
}

const porsonName = 'Alexey';
const personAge = -10;

console.log(logPerson('s', 'alex', 22));
const output = logPerson`Имя: ${porsonName}, Возрост: ${personAge}`;
const output2 = logPerson`Имя: ${porsonName}, Возрост: ${personAge}`;

console.log(output);
console.log(output2); */